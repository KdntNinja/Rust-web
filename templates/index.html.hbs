{{#*inline "page"}}
    <style>
        /* Homepage specific styles */
        .hero-section {
            background-image: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            position: relative;
            overflow: hidden;
        }
        .hero-section::before {
            content: '';
            position: absolute;
            top: -50px;
            right: -50px;
            width: 200px;
            height: 200px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            z-index: 0;
        }
        .service-card {
            transition: all 0.3s ease;
        }
        .service-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        }
        .feature-icon {
            transition: transform 0.3s ease;
        }
        .feature-card:hover .feature-icon {
            transform: scale(1.2);
        }
    </style>

    <div class="py-6">
        <!-- Hero Section -->
        <div class="hero-section text-white rounded-xl shadow-xl p-10 mb-12">
            <div class="grid md:grid-cols-2 gap-8 items-center">
                <div>
                    <h1 class="text-4xl md:text-5xl font-bold mb-4">Homework Done Automatically</h1>
                    <p class="text-xl mb-6">Subscribe once and our AI takes care of the rest. We automatically complete all your assignments from SparxMaths, Educake, and LanguageGym without you lifting a finger.</p>
                    <div class="flex flex-wrap gap-4">
                        <a href="/signup" class="inline-flex items-center bg-white text-indigo-600 hover:bg-gray-100 py-3 px-6 rounded-lg font-semibold transition-colors duration-200">
                            <i class="fas fa-rocket mr-2"></i> Start Your Subscription
                        </a>
                        <a href="/pricing" class="inline-flex items-center border-2 border-white text-white hover:bg-white hover:text-indigo-600 py-3 px-6 rounded-lg font-semibold transition-colors duration-200">
                            <i class="fas fa-tags mr-2"></i> View Plans
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Services Section -->
        <div class="mb-12">
            <h2 class="text-3xl font-bold text-gray-800 mb-6 text-center">Our Automated Services</h2>
            <div class="grid md:grid-cols-3 gap-6">
                <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition-shadow duration-300 service-card">
                    <div class="text-indigo-600 text-4xl mb-4">
                        <i class="fas fa-calculator"></i>
                    </div>
                    <h3 class="text-xl font-semibold text-gray-800 mb-2">SparxMaths Automation</h3>
                    <p class="text-gray-600">Our system monitors your SparxMaths account and automatically completes all assignments with perfect accuracy.</p>
                </div>
                <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition-shadow duration-300 service-card">
                    <div class="text-indigo-600 text-4xl mb-4">
                        <i class="fas fa-book"></i>
                    </div>
                    <h3 class="text-xl font-semibold text-gray-800 mb-2">Educake Auto-Completion</h3>
                    <p class="text-gray-600">Connect your Educake account once, and we'll handle all your assignments automatically as they're assigned.</p>
                </div>
                <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition-shadow duration-300 service-card">
                    <div class="text-indigo-600 text-4xl mb-4">
                        <i class="fas fa-language"></i>
                    </div>
                    <h3 class="text-xl font-semibold text-gray-800 mb-2">LanguageGym Automation</h3>
                    <p class="text-gray-600">Our system monitors and completes all LanguageGym exercises, ensuring you maintain perfect scores effortlessly.</p>
                </div>
            </div>
        </div>

        <!-- Features Section -->
        <div class="bg-gray-50 rounded-xl p-10 mb-12">
            <h2 class="text-3xl font-bold text-gray-800 mb-8 text-center">Why Choose Our Subscription Service</h2>
            <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
                <div class="bg-white p-6 rounded-lg shadow-sm feature-card">
                    <div class="text-indigo-600 text-3xl mb-3 feature-icon">
                        <i class="fas fa-robot"></i>
                    </div>
                    <h3 class="font-semibold text-lg mb-2">Fully Automated</h3>
                    <p class="text-gray-600">Set it and forget it. Our system works silently in the background.</p>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-sm">
                    <div class="text-indigo-600 text-3xl mb-3 feature-icon">
                        <i class="fas fa-clock"></i>
                    </div>
                    <h3 class="font-semibold text-lg mb-2">24/7 Monitoring</h3>
                    <p class="text-gray-600">We check for new assignments around the clock and complete them on time.</p>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-sm">
                    <div class="text-indigo-600 text-3xl mb-3 feature-icon">
                        <i class="fas fa-award"></i>
                    </div>
                    <h3 class="font-semibold text-lg mb-2">Perfect Scores</h3>
                    <p class="text-gray-600">Our algorithms ensure you consistently receive top grades.</p>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-sm">
                    <div class="text-indigo-600 text-3xl mb-3 feature-icon">
                        <i class="fas fa-shield-alt"></i>
                    </div>
                    <h3 class="font-semibold text-lg mb-2">Secure & Private</h3>
                    <p class="text-gray-600">Your account credentials are encrypted and never shared.</p>
                </div>
            </div>
        </div>

        <!-- CTA Section -->
        <div class="bg-indigo-100 rounded-xl p-10 text-center">
            <h2 class="text-3xl font-bold text-indigo-800 mb-4">Ready to Automate Your Homework?</h2>
            <p class="text-xl text-indigo-600 mb-6">Subscribe today and never worry about homework deadlines again!</p>
            <a href="/signup" class="inline-flex items-center bg-indigo-600 hover:bg-indigo-700 text-white py-3 px-8 rounded-lg font-semibold transition-colors duration-200">
                <i class="fas fa-rocket mr-2"></i> Start Your Subscription
            </a>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // Add some engaging animations to service cards
            const serviceCards = document.querySelectorAll('.service-card');
            serviceCards.forEach((card, index) => {
                setTimeout(() => {
                    card.classList.add('animate-pulse');
                    setTimeout(() => {
                        card.classList.remove('animate-pulse');
                    }, 500);
                }, index * 300);
            });
            
            // Animate feature icons on scroll
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.transform = 'scale(1.1)';
                        setTimeout(() => {
                            entry.target.style.transform = 'scale(1)';
                            entry.target.style.transition = 'transform 0.5s ease';
                        }, 300);
                        observer.unobserve(entry.target);
                    }
                });
            }, { threshold: 0.5 });
            
            document.querySelectorAll('.feature-icon').forEach(icon => {
                observer.observe(icon);
            });
        });
    </script>
{{/inline}}
{{~> layouts/base title="Automatic Homework Completion - SparxMaths, Educake, LanguageGym"~}}
